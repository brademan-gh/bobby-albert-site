// Place any global data in this file.
// You can import this data from anywhere in your site by using the `import` keyword.

export const SITE_TITLE = 'Bobby Albert';
export const SITE_DESCRIPTION =
	'Bobby Albert on leadership and workplace culture — how the way you lead shapes what your business becomes.';

// While the site is under review, keep it out of search results.
//
// Note: a robots.txt "Disallow" is NOT the right tool here. It stops crawlers
// *fetching* pages, so they never see a noindex tag — and Google can still
// list a bare URL it found elsewhere. Serving `noindex` while allowing the
// crawl is what actually keeps pages out of results.
//
// Set to false to open the site to search engines.
export const NOINDEX = true;
