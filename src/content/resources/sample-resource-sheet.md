---
title: "Sample Resource — Replace With a Real One"
description: "A placeholder demonstrating the resource entry frontmatter format."
sourcePdf: "sample-resource-sheet"
category: "Sample"
dateConverted: 2026-07-27
draft: true
---

This is a placeholder info sheet. The `sourcePdf` field's value (without the
`.pdf` extension) must exactly match both this file's slug and the PDF's
filename in `public/pdfs/`, e.g. `public/pdfs/sample-resource-sheet.pdf`.

Replace this body with the HTML/Markdown conversion of the real PDF content,
and drop the actual PDF file into `public/pdfs/` using the matching filename.
